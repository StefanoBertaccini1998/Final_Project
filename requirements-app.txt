# Runtime dependencies for the Gradio demo (CPU-only, Railway deployment).
# PyTorch CPU keeps the Docker image ~1.5 GB instead of ~4 GB.
# Install with:
#   pip install -r requirements-app.txt \
#       --extra-index-url https://download.pytorch.org/whl/cpu

# Core CV / ML
opencv-python-headless>=4.8.0
numpy>=1.24.0
scikit-learn>=1.3.0
scikit-image>=0.21.0
matplotlib>=3.7.0
Pillow>=10.0.0
joblib>=1.3.0

# PyTorch CPU
torch>=2.6.0
torchvision>=0.21.0

# Grad-CAM
grad-cam>=1.5.0

# Demo interface
gradio>=4.44.0
